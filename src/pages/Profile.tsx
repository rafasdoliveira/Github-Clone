import React from 'react';
import Header from '../components/Header/Header';

const Profile: React.FC = () => {
  return (
    <div className="h-screen bg-profileBg">
      <Header />
    </div>
  );
};

export default Profile;
